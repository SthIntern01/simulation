services:
  - type: web
    name: phishing-tracker
    env: node
    plan: free
    buildCommand: npm ci --prefer-offline --no-audit
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
